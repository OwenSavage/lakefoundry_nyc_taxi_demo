command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8080"
  - --server.address
  - 0.0.0.0
  - --server.headless
  - "true"

env:
  - name: PYTHONPATH
    value: /app:/app/src
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: sql-warehouse
