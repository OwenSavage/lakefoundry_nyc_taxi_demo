command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8080"
  - --server.address
  - 0.0.0.0
  - --server.headless
  - "true"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: sql-warehouse
